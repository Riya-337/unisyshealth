{
  "_comment": "backend: 'sentinel' (SSE zero-cloud default) | 'console' (tests only). Note: 'telegram' / 'dual' are blocked by zero-cloud runtime guard.",

  "backend": "sentinel",
  "sentinel": {
    "_comment": "SentinelNotifier — self-hosted SSE zero-cloud. Alerts go to server console + logs/alert_queue.jsonl + SSE stream (/api/stream) for local desktop subscriber."
  },
  "telegram": {
    "_comment": "Disabled by zero-cloud policy. Outbound calls to Telegram API are blocked.",
    "bot_token_env": "TELEGRAM_BOT_TOKEN",
    "chat_id_env": "TELEGRAM_CHAT_ID"
  }
}

